.gallery
  .row
    .col-lg-9.col-lg-offset-3
      - if @photos.empty?
        h5
          ' Мы не можем найти фото для
          strong= @search_tag
          |. Измените параметры поиска.
      - elsif @search_tag.blank?
        h5 Вы не ввели слова для поиска.
      - else
        h5
          ' Вот что мы нашли для слова
          strong= @search_tag
          |.
  .row
    .photo-grid
      - @photos.each do |photo|
        - class_offset = "col-xs-offset-1" if @photos.index(photo) == 0 || @photos.index(photo) == 5
        .col-xs-5.col-md-2 class=class_offset
          a.thumbnail.thumbnail-photo href='#' id="thumbnail-#{@photos.index(photo)}"
            = image_tag photo, id: "photo-#{@photos.index(photo)}"
